# Codex 작업 원칙

이 볼트에서 작업할 때는 아래 원칙을 우선한다. 특히 옵시디언 마크다운 노트, 링크, MOC, 대표 문서, 에버그린 노트 정리 작업에서는 자동화보다 수동 판단을 우선한다.

## 기본 원칙

1. 함부로 자동화하여 일괄 처리하지 않는다.
   - 파이썬 스크립트나 정규식 일괄 치환으로 볼트 전체를 수정하지 않는다.
   - 대량 삭제, 대량 링크 변경, 대량 이름 변경은 사용자가 명시적으로 요청하고 범위를 확인한 경우에만 진행한다.
   - 검색 명령은 후보 파악용으로만 사용하고, 실제 수정은 파일 내용을 직접 읽고 판단한 뒤 한다.

2. 작업을 시작할 때는 샘플 하나를 먼저 처리한다.
   - 같은 패턴으로 여러 파일을 고쳐야 할 때도 먼저 대표 샘플 1개만 수정한다.
   - 샘플 처리 결과를 보고한 뒤, 같은 방식으로 계속 진행해도 되는지 사용자에게 확인한다.
   - 사용자가 "계속 진행"을 명확히 말하기 전에는 같은 패턴을 대량 적용하지 않는다.

3. 했던 작업을 다시 하지 않도록 한 번에 제대로 처리한다.
   - 파일을 수정하기 전에 현재 내용, 기존 링크, 중복 문서, 대표 문서 여부를 확인한다.
   - 링크를 추가할 때는 대상 파일이 실제로 존재하는지 확인한다.
   - 미존재 링크를 새로 만들지 않는다. 필요하지만 문서가 없으면 일반 텍스트로 남기거나 사용자에게 확인한다.
   - 작업 후에는 수정한 파일만 좁게 확인한다. 볼트 전체 자동 정리는 하지 않는다.

4. 요청하지 않은 작업을 함부로 진행하지 않는다.
   - 사용자가 요청한 범위 밖의 문서, 대표 문서, 이미 복구된 문서는 건드리지 않는다.
   - 사용자가 "대표 문서 건들지마"라고 한 경우 대표 문서는 읽기만 하고 수정하지 않는다.
   - 처리 사항 보강이 필요하면 추측해서 진행하지 말고 질문한다.

## 옵시디언 볼트 작업 규칙

- `[[...]]`는 옵시디언 내부 링크 문법이다. 깨진 데이터로 판단하지 않는다.
- 링크 대상이 존재하지 않는다고 해서 자동으로 문서를 만들거나 링크를 제거하지 않는다. 먼저 그 링크가 중요한 개념인지, 기존 대표 문서로 통합 가능한지 확인한다.
- 같은 개념의 중복 문서는 대표 문서를 기준으로 통합하되, 대표 문서를 수정해야 하면 먼저 사용자에게 확인한다.
- 저품질 PDF 파편 노트는 삭제, 텍스트 격하, 수동 재작성 중 하나를 선택하되 파일 내용을 직접 읽고 판단한다.
- 에버그린 노트로 재작성할 때는 기존 작성 가이드를 따르되, 자료가 부족하면 억지로 작성하지 않고 사용자에게 보고한다.

## 금지 사항

- 볼트 전체를 대상으로 한 자동 수정 스크립트 실행 금지.
- 후보 목록 전체를 한 번에 삭제하거나 치환 금지.
- 미확인 링크 생성 금지.
- 대표 문서 무단 수정 금지.
- 사용자가 직접 복구한 파일 재수정 금지.
